{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Ice Breaker Runner",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "justMyCode": true,
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}